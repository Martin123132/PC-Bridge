# Contributing

PC Bridge is currently an early public preview.

Before submitting a change:

1. keep changes narrowly scoped;
2. run `npm test`;
3. run `npm run verify:release`;
4. do not commit local connector state, credentials, logs, private paths or generated recovery material;
5. preserve third-party licence and notice files.

Security-sensitive changes should be discussed privately before being opened publicly.
